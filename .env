MONGO_URI=mongodb+srv://dhivakarm:Dhivakarm2005@cluster0.3wzgrcx.mongodb.net/devs_clinic?appName=Cluster0
MONGO_DB_NAME=devs_clinic
DJANGO_SECRET_KEY=change-this-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Email notifications — sent to clinic staff whenever a new enquiry/booking is submitted.
# Using Resend's SMTP relay (https://resend.com).
# EMAIL_HOST_USER must be the literal word "resend" (that's Resend's fixed SMTP username).
# EMAIL_HOST_PASSWORD is your Resend API key (starts with "re_") — get it from
#   https://resend.com/api-keys. Treat it like a password, never commit it.
# DEFAULT_FROM_EMAIL must be an address on a domain you've verified in Resend,
#   OR the shared test address "onboarding@resend.dev" (works immediately, no
#   domain verification needed, but can only deliver to the email address you
#   signed up to Resend with until you verify your own domain).
# CLINIC_NOTIFICATION_EMAIL: where enquiry/booking alerts are delivered.
EMAIL_HOST=smtp.resend.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=resend
EMAIL_HOST_PASSWORD=re_BXJ7zLLx_4doArGroJpkoWXtr74DedHa7
DEFAULT_FROM_EMAIL=onboarding@resend.dev
CLINIC_NOTIFICATION_EMAIL=mdhivakar091@gmail.com
